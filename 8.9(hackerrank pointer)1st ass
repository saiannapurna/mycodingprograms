#include <stdio.h>
#include<stdlib.h>
void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
    int *p,*q;
    p=&a;
    q=&b;
    scanf("%d %d",&a,&b);
    
    printf("%d\n%d",abs(*p+*q),abs(*p-*q));
    
    return 0;
}
