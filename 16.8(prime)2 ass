#include<stdio.h>
int main()
{
	int a[4],i,flag,j;
	printf("elements are");

	for(i=0;i<5;i++)
	{
		scanf("%d",&a[i]);
	}
		printf("numbers are :");
	for(i=0;i<5;i++)
	{
		flag=0;
		for(j=2;j<a[i];j++)
		{
			if(a[i]%j==0)
			{
				flag=1;
				break;
			}
		}
		if(flag==0)
		printf(" %d  ",a[i]);
	}
	
	return 0;
}
