#include<stdio.h>
int main()
{
	int arr[]={2,3,4,5};
	int len=sizeof(arr)/sizeof(int);
	int i,n,h=0;
	for(i=1;i<len;i++)
	{
	    	 arr[i]=n;
	    	 while(n>0){
	    if(arr[i]==n)
	    h=1;
	    n--;
	}
	}
	if(h==1)
		printf("DUPLICATE");
     else
        printf("UNIQUE");
	return 0;

	
}
